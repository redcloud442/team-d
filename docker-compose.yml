services:
  traefik:
    image: traefik:v3.1
    command:
      - "--providers.docker"
      - "--providers.docker.exposedbydefault=false"
      - "--entryPoints.websecure.address=:443"
      - "--certificatesresolvers.myresolver.acme.tlschallenge=true"
      - "--certificatesresolvers.myresolver.acme.email=adminuser@redful.xyz"
      - "--certificatesresolvers.myresolver.acme.storage=/letsencrypt/acme.json"
      - "--entrypoints.web.address=:80"
      - "--entrypoints.web.http.redirections.entrypoint.to=websecure"
      - "--entrypoints.web.http.redirections.entrypoint.scheme=https"
    ports:
      - mode: host
        protocol: tcp
        published: 80
        target: 80
      - mode: host
        protocol: tcp
        published: 443
        target: 443
    volumes:
      - letsencrypt:/letsencrypt
      - /var/run/docker.sock:/var/run/docker.sock

  web:
    image: ghcr.io/redcloud442/red:prod
    labels:
      - "traefik.enable=true"
      - "traefik.http.services.red-app.loadbalancer.server.port=8080"
      - "traefik.http.routers.red-app.rule=Host(`redful.xyz`)"
      - "traefik.http.routers.red-app.entrypoints=websecure"
      - "traefik.http.routers.red-app.tls.certresolver=myresolver"
    secrets:
      - databaseUrl
      - directUrl
      - supabaseUrl
      - anon
      - cryptokey
      - role
    environment:
      - DATABASE_URL_FILE=/run/secrets/databaseUrl
      - DIRECT_URL_FILE=/run/secrets/directUrl
      - NEXT_PUBLIC_SUPABASE_URL_FILE=/run/secrets/supabaseUrl
      - NEXT_PUBLIC_SUPABASE_ANON_KEY_FILE=/run/secrets/anon
      - NEXT_PUBLIC_CRYPTO_SECRET_KEY_FILE=/run/secrets/cryptokey
      - SUPABASE_SERVICE_ROLE_KEY_FILE=/run/secrets/role
    deploy:
      mode: replicated
      replicas: 3
      update_config:
        order: start-first

volumes:
  letsencrypt:
secrets:
  databaseUrl:
    external: true
  directUrl:
    external: true
  supabaseUrl:
    external: true
  anon:
    external: true
  cryptokey:
    external: true
  role:
    external: true
